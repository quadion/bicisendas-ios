stages:
  - build
  - archive

build_project:
  stage: build
  script:
    - bundle install
    - security unlock-keychain -p quadion ~/Library/Keychains/Quadion.keychain
    - bundle exec fastlane build
